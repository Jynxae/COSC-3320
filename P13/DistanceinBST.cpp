#include <iostream>
#include <vector>
#include<fstream>
using namespace std;

struct node{
    int value;
    node* right;
    node* left;
    node* next;
};

void insert(int val)
{
    node* temp = nullptr;
    temp->value = val;
    temp->next = nullptr;

    
}

int main(){

return 0;
}